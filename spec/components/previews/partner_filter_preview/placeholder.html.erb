<div style="padding: 2rem; font-family: system-ui, sans-serif;">
  <h3>PartnerFilter</h3>
  <p>This component requires database records to render (it queries
  <code>PartnersQuery</code> for categories and neighbourhoods).</p>
  <p>To preview it, visit <code>/partners</code> on a running site with seed data.</p>
</div>
