<li class="event">
  <h3><%= link_to properties[:summary], event_path(properties[:id]) %></h3>
  <p>
      🕒 <time><%= dtstart %> &mdash; <%= dtend %></time>
      📍 <% if properties[:place] %>
          <%= link_to properties[:place], place_path(properties[:place]) %>
        <% else %>
          <%= properties[:location].split(",").first.gsub("\\", "") %>
        <% end %>
  </p>
</li>
