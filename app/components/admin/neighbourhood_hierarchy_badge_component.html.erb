<div class="inline-flex flex-wrap items-center gap-1">
  <% if truncated? %>
    <span class="text-gray-400 text-sm">...</span>
    <span class="text-gray-300">/</span>
  <% end %>

  <% hierarchy_items.each_with_index do |item, index| %>
    <% if index > 0 %>
      <span class="text-gray-300">/</span>
    <% end %>

    <% if link_each %>
      <%= link_to helpers.admin_neighbourhood_path(item),
          class: "inline-flex items-center gap-1.5 #{badge_size_classes} rounded #{level_colour(item.level)} hover:opacity-80 transition-opacity" do %>
        <% if show_icons %>
          <span class="inline-flex items-center justify-center w-4 h-4 rounded-full bg-current/10 text-[9px] font-bold">L<%= item.level %></span>
        <% end %>
        <%= item.shortname %>
      <% end %>
    <% else %>
      <span class="inline-flex items-center gap-1.5 <%= badge_size_classes %> rounded <%= level_colour(item.level) %>">
        <% if show_icons %>
          <span class="inline-flex items-center justify-center w-4 h-4 rounded-full bg-current/10 text-[9px] font-bold">L<%= item.level %></span>
        <% end %>
        <%= item.shortname %>
      </span>
    <% end %>
  <% end %>
</div>
