<header class="header" data-controller="mobile-menu">
  <%= link_to(root_path, class:'header__branding') do %>
    <% if @logo_path %>
      <% if @logo_path =~ /\.svg$/ %>
        <%= svg_image(@logo_path, alt_text: @site.name, css_class: "header__branding header__branding--#{ @site.slug }") %>
      <% else %>
        <%= image_tag(@logo_path, alt: @site.name, class: "header__branding header__branding--#{ @site.slug }") %>
      <% end %>
    <% else %>
      <%= svg_image('home/icons/logo.svg', alt_text: 'PlaceCal', css_class: "header__branding header__branding--#{ @site.slug }") %>
    <% end %>
    <% if @site&.default_site? %>
      <% if request.path == '/' %>
        <h1>PlaceCal</h1>
      <% else %>
        <h2>PlaceCal</h2>
      <% end %>
    <% else %>
      <h2><%= @site&.name %></h2>
      <p>The Community Calendar</p>
    <% end %>
  <% end %>
  <nav class="nav header__menu" data-mobile-menu-target="menu" role="navigation">
    <ul>
      <li><%= active_link_to "Home", root_path %></li>
      <% navigation.each do |link_text, link_path| %>
        <li><%= active_link_to link_text, link_path, data: { turbolinks: false } %></li>
      <% end %>
    </ul>
  </nav>
  <button type="button" class="header__toggle" data-action="click->mobile-menu#toggle" data-turbo="false">
    Menu
    <%= icon(:misc_menu, size:'0') %>
  </button>
</header>
