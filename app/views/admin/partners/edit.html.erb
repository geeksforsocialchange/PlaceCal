<% content_for :title do %>Edit Partner: <%= @partner.name %><% end %>

<%= render(Admin::ErrorComponent.new(@partner)) %>

<div class="flex items-center justify-between mb-6">
  <div>
    <h1 class="text-2xl font-semibold">Edit Partner</h1>
    <p class="text-gray-600 mt-1"><%= @partner.name %></p>
  </div>
  <div class="text-sm text-gray-600">ID: <%= @partner.id %></div>
</div>

<%= render 'form', model: :partner %>
