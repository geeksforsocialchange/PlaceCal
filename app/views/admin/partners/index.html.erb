<%# TODO: Only show Edit link for users with access %>

<%= render_component "admin_index",
  title: "Partners",
  model: :partners,
  columns: %i[id name slug address],
  data: @partners,
  new_link: current_user.neighbourhood_admin? ? setup_admin_partners_path : new_admin_partner_path
%>
