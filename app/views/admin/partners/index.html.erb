<script>
var columns = [
    {"data": "id"},
    {"data": "name"},
    {"data": "slug"},
    {"data": "address"}
  ]
</script>

<%= render partial: "layouts/admin/datatable", locals: {
    title: "Partners",
    model: :partners,
    columns: %i[id name slug address],
    data: @partners,
    source: admin_partners_path(format: :json),
    new_link: new_admin_partner_path
  }
%>
