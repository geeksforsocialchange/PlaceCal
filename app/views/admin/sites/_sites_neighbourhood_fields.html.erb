<% if f.object.relation_type != 'Primary' %>
  <% if f.object.neighbourhood.present? %>
    <%= render Views::Admin::Components::NeighbourhoodCard.new(
          neighbourhood: f.object.neighbourhood,
          show_header: false,
          show_remove: true,
          form: f
        ) %>
  <% else %>
    <%= render Views::Admin::Components::CascadingNeighbourhoodFields.new(form: f, relation_type: 'Secondary', show_remove: true) %>
  <% end %>
<% end %>
