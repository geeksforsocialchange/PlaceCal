<%= simple_form_for [:admin, @site], html: { data: { controller: 'site-tabs live-validation' } } do |f| %>
  <%= render(Admin::ErrorComponent.new(@site)) %>

  <%# Radio Tabs - daisyUI tabs-lift %>
  <div role="tablist" class="tabs tabs-lift">
    <input type="radio" name="site_tabs" class="tab" aria-label="📋 Basic Info" data-site-tabs-target="tab" data-hash="basic" checked="checked" />
    <div class="tab-content bg-base-100 border-base-300 p-6" data-site-tabs-target="panel" data-section="basic">
      <%= render 'form_tab_basic', f: f %>
    </div>

    <input type="radio" name="site_tabs" class="tab" aria-label="🖼️ Images" data-site-tabs-target="tab" data-hash="images" />
    <div class="tab-content bg-base-100 border-base-300 p-6" data-site-tabs-target="panel" data-section="images">
      <%= render 'form_tab_images', f: f %>
    </div>

    <input type="radio" name="site_tabs" class="tab" aria-label="📍 Neighbourhoods" data-site-tabs-target="tab" data-hash="neighbourhoods" />
    <div class="tab-content bg-base-100 border-base-300 p-6" data-site-tabs-target="panel" data-section="neighbourhoods">
      <%= render 'form_tab_neighbourhoods', f: f %>
    </div>

    <input type="radio" name="site_tabs" class="tab" aria-label="🏷️ Partnerships" data-site-tabs-target="tab" data-hash="partnerships" />
    <div class="tab-content bg-base-100 border-base-300 p-6" data-site-tabs-target="panel" data-section="partnerships">
      <%= render 'form_tab_partnerships', f: f %>
    </div>

    <% unless @site.new_record? %>
      <input type="radio" name="site_tabs" class="tab" aria-label="👁️ Preview" data-site-tabs-target="tab" data-hash="preview" />
      <div class="tab-content bg-base-100 border-base-300 p-6" data-site-tabs-target="panel" data-section="preview">
        <%= render 'form_tab_preview' %>
      </div>
    <% end %>

    <%# Spacer to push Admin tab right %>
    <div class="tab flex-1 cursor-default"></div>

    <input type="radio" name="site_tabs" class="tab" aria-label="⚙ Admin" data-site-tabs-target="tab" data-hash="admin" />
    <div class="tab-content bg-base-100 border-base-300 p-6" data-site-tabs-target="panel" data-section="admin">
      <%= render 'form_tab_admin', f: f %>
    </div>
  </div>

  <%= render Admin::SaveBarComponent.new(
    multi_step: true,
    tab_name: 'site_tabs',
    settings_hash: 'admin',
    preview_hash: 'preview',
    storage_key: 'siteTabAfterSave'
  ) %>
<% end %>
