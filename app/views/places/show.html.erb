<%= render_component "place", context: :page,
                              id: @place.id,
                              name: @place.name,
                              address: @place.address,
                              description: @place.short_description %>

