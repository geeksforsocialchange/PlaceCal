<nav class="fixed top-0 left-0 right-0 h-14 bg-gray-900 shadow-lg flex items-center justify-between px-4 z-50">
  <%= link_to 'PlaceCal', admin_root_path, class: "text-white font-bold text-lg hover:text-placecal-orange transition-colors" %>
  <ul class="flex items-center gap-4">
    <li>
      <%= link_to admin_profile_path, class: 'text-gray-300 hover:text-white transition-colors flex items-center gap-2' do %>
        <i class='fa fa-user'></i>
        <span><%= @current_user&.email %> <%= "👑" if @current_user&.role == :root %></span>
      <% end %>
    </li>
    <li>
      <%= button_to destroy_user_session_path, method: :delete, class: 'text-gray-300 hover:text-white transition-colors flex items-center gap-2', form: { data: { turbo: 'false' } } do %>
        <i class='fa fa-sign-out'></i>
        <span>Sign out</span>
      <% end %>
    </li>
  </ul>
</nav>
